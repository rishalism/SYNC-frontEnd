import React from 'react';

const SmallScreenMessage = () => (
    <div style={{ padding: '20px', textAlign: 'center' }}>
        <h2>Please use a large screen device for better experience</h2>
        <p>This application is optimized for laptops and desktop computers.</p>
    </div>
);

export default SmallScreenMessage;